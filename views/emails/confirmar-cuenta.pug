html(lang="es")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        style 
            include ./email.css

    body
        h2 Confirma tu cuenta Uptask
        
        p ya estas casi inscripto solo continua presionando en el siguiente enlace
        a(href=`${url}`) Ir a Confirmar cuenta Uptask
        p si no puede acceder a este enlace, visita #{url}
        p si no solicitaste este e-mail, no puedes ingnorarlo